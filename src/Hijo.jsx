//import React, { useEffect, useState } from "react";

//import { useState } from "react";

function Hijo({ handleChange }) {
  return (
    <div>
      Componente hijo:
      <h4>{handleChange}</h4>
    </div>
  );
}
export default Hijo;
